/** Consistent spacing rhythm (mobile). Mirrors the web design system's spacing conventions. */
export const spacing = {
  xs: 4,
  sm: 8,
  md: 12,
  lg: 16,
  xl: 20,
  xxl: 24,
  xxxl: 32,
};

/** Corner radii — 18px for major cards, 13px for buttons/inputs, 10px for chips/badges. */
export const radii = {
  lg: 18,
  md: 13,
  sm: 10,
  full: 999,
};
